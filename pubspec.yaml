name: kvs
description: High performance persistant memory based key-value storage with AES256 encryption. Support all features and can be used as drop-in of Dart Map<K,V>.
version: 0.0.4
homepage: https://ensorta.com
repository: https://github.com/hsbijarniya/kvs

environment:
  sdk: ">=2.17.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.15
  cryptography: ^2.5.0
  cryptography_flutter: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
